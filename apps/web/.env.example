NEXT_PUBLIC_NETWORK_LABEL="Base chain"
NEXT_PUBLIC_CHAIN_ID="8453"
# WalletConnect Project ID from https://cloud.walletconnect.com/
# Required for wallet connection modal to function properly
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=""
NEXT_PUBLIC_TREASURY_SAFE_ADDRESS=""
NEXT_PUBLIC_SIGNAL_SINK_ADDRESS=""
NEXT_PUBLIC_REPUTATION_REGISTRY_ADDRESS=""

# Last run metadata (used in the UI + verification card)
NEXT_PUBLIC_LAST_RUN_STATUS="pass"
NEXT_PUBLIC_LAST_RUN_TS="2026-02-05T00:30:00Z"
NEXT_PUBLIC_LAST_RUN_NETWORK="Base fork"
NEXT_PUBLIC_LAST_RUN_LOG="fork-smoke: direct transfer 0.05 WETH to burn sink OK"

# Optional fallback label (legacy)
NEXT_PUBLIC_LAST_RUN_LABEL="pending"

# Optional JSON feed URL for pulse rows
NEXT_PUBLIC_PULSE_FEED_URL="/pulse-feed.json"

# Optional explorer base URL for tx links
NEXT_PUBLIC_EXPLORER_TX_BASE_URL="https://basescan.org/tx/"

# Pulse token + sink
NEXT_PUBLIC_PULSE_TOKEN_ADDRESS=""
NEXT_PUBLIC_SIGNAL_SINK_ADDRESS=""
NEXT_PUBLIC_PULSE_REGISTRY_ADDRESS=""

# Alive window + inbox key TTL
NEXT_PUBLIC_ALIVE_WINDOW_SECONDS="86400"
ALIVE_WINDOW_SECONDS="86400"
NEXT_PUBLIC_INBOX_KEY_TTL_SECONDS="3600"

# Optional block confirmations for log reads
BLOCK_CONFIRMATIONS="2"

# Base RPC URL for CLIENT components (exposed to browser)
# Use only for client-side blockchain reads
NEXT_PUBLIC_BASE_RPC_URL="https://mainnet.base.org"
NEXT_PUBLIC_IDENTITY_REGISTRY_ADDRESS=""
NEXT_PUBLIC_ERC8004_REGISTER_URL="https://www.8004.org"

# ERC-8004 gate (optional; default OFF)
REQUIRE_ERC8004="false"

# Server-only Base RPC URL for API routes (H-4 security fix)
# This is used by all server-side API routes (app/api/**) to prevent
# exposing RPC URLs to clients. Keep this secret - do not prefix with NEXT_PUBLIC_.
# If not set, API routes will fail closed (return error) rather than fall back to public URL.
BASE_RPC_URL="https://mainnet.base.org"
PULSE_TOKEN_ADDRESS=""
SIGNAL_SINK_ADDRESS=""
INBOX_KEY_TTL_SECONDS="3600"
BLOCK_TIME_SECONDS="2"

# Server-only (for deploy/verify + KV cache)
PRIVATE_KEY=""
ETHERSCAN_API_KEY=""
KV_URL=""
KV_REST_API_URL=""
KV_REST_API_TOKEN=""

# ===== x402 Micropayment Integration =====
# Facilitator URL (testnet: https://x402.org/facilitator, mainnet: use Thirdweb or self-hosted)
X402_FACILITATOR_URL="https://x402.org/facilitator"
# Token metadata for EIP-712 permit signing
PULSE_TOKEN_NAME="Agent Pulse"
PULSE_TOKEN_VERSION="1"
# Amount of PULSE tokens per pulse (1 PULSE = 1e18)
PULSE_AMOUNT="1000000000000000000"

# ===== Thirdweb (for Insight API event querying) =====
# Get from https://thirdweb.com/dashboard/settings/api-keys
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=""
# Optional: server-side secret key (more secure than client ID)
THIRDWEB_SECRET_KEY=""

# ===== HeyElsa DeFi API (optional, for $1000 bonus) =====
# HeyElsa endpoints are protected by x402 and require your SERVER to pay HeyElsa in USDC.
# Configure a dedicated hot wallet private key that holds Base USDC.
# NOTE: server-only secret (do NOT prefix with NEXT_PUBLIC_)
HEYELSA_PAYMENT_KEY=""
# HeyElsa x402 API base URL
HEYELSA_X402_API_URL="https://x402-api.heyelsa.ai"

# Paid endpoint demo bypass (optional)
# If set, requests with header `X-API-KEY: <PAID_API_BYPASS_KEY>` will skip incoming x402 payment checks.
# Useful for hackathon demos / judges (underlying HeyElsa calls still use server-paid x402).
PAID_API_BYPASS_KEY=""

# Server wallet to receive x402 payments for /api/paid/* endpoints (defaults to Treasury Safe address)
SERVER_WALLET_ADDRESS=""

# Protocol Verification Dashboard
NEXT_PUBLIC_TESTNET_RPC_URL="https://sepolia.base.org"
NEXT_PUBLIC_TESTNET_REGISTRY_ADDRESS=""
NEXT_PUBLIC_TESTNET_TOKEN_ADDRESS=""
NEXT_PUBLIC_FORK_RPC_URL="http://127.0.0.1:8545"
NEXT_PUBLIC_FORK_REGISTRY_ADDRESS=""
NEXT_PUBLIC_FORK_TOKEN_ADDRESS=""
NEXT_PUBLIC_VERIFY_LOG_TESTNET_URL=""
NEXT_PUBLIC_VERIFY_LOG_FORK_URL=""
NEXT_PUBLIC_VERIFY_LOG_MAINNET_URL=""

# Desk Watch (optional) — agent addresses must live in env vars, not source
NEXT_PUBLIC_DESK_WATCH_DESK_VC_AGENT=""
NEXT_PUBLIC_DESK_WATCH_DESK_CB_AGENT=""
NEXT_PUBLIC_DESK_WATCH_DESK_03_AGENT=""
