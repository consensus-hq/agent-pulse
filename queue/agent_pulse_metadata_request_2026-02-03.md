Subject: Agent Pulse — token metadata + deploy prerequisites

Quick confirms to unblock the $PULSE deploy + demo:
1) Token description (1–2 sentences) + socials (website/X/Discord, etc.).
2) Sink address confirmation (Treasury Safe default? confirm which Safe).
3) Connie Core EOA funding status on Base (approx amount + ETA).
4) Vercel deploy URL status for apps/web (PR #22 preferred, fallback feat-hackathon-demo-ui).

Once you confirm, I’ll finalize the Clanker deploy config + proceed with the UI feed wiring.